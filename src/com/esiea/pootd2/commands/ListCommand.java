package com.esiea.pootd2.commands;

/**
 * Represents the 'ls' command to list the content of the current directory.
 */
public class ListCommand extends Command {
    // This class is empty because 'ls' doesn't need any arguments (like a name or path).
}