package com.esiea.pootd2.commands;

/**
 * Base class for all application commands (ls, mkdir, cd, etc.).
 * It acts as a common type so the controller can handle any command generically.
 */
public abstract class Command {
    // Intentionally empty. Specific commands will add their own data fields.
}